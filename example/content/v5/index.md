# somesoftware v5

Major rewrite documentation.

## Breaking Changes from v3

- Config format changed from TOML to YAML
- Plugin API v1 removed, only v2 supported
- `/api/process` renamed to `/api/jobs`

## Architecture

Complete rewrite using gRPC internally between services.
