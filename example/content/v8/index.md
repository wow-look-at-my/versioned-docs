# somesoftware v8

Latest documented version.

## What's New Since v5

- Added WebSocket support for real-time job status
- New `/api/ws` endpoint
- Prometheus metrics on `/metrics`
- Config now supports environment variable overrides with `SS_` prefix
